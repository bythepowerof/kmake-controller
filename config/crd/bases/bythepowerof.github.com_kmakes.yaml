
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: kmakes.bythepowerof.github.com
spec:
  group: bythepowerof.github.com
  names:
    kind: Kmake
    listKind: KmakeList
    plural: kmakes
    singular: kmake
  scope: ""
  validation:
    openAPIV3Schema:
      description: Kmake is the Schema for the kmakes API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: KmakeSpec defines the desired state of Kmake
          properties:
            rules:
              items:
                properties:
                  commands:
                    items:
                      type: string
                    type: array
                  doublecolon:
                    type: boolean
                  prereqs:
                    items:
                      type: string
                    type: array
                  targets:
                    items:
                      type: string
                    type: array
                required:
                - commands
                - doublecolon
                - prereqs
                - targets
                type: object
              type: array
            variables:
              additionalProperties:
                type: string
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file
                Variables []KmakeVariable `json:"variables,omitempty"`'
              type: object
          type: object
        status:
          description: KmakeStatus defines the observed state of Kmake
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
